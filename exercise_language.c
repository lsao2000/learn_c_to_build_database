// make a program to calculate array of character if it is the longest array
// save it and save its length to print it at the end of the program.
#include <stdio.h>
int main(int argc, char *argv[]) {
  // while (1) {
  //     getchar();
  // }
  return 0;
}
